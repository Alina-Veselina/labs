//5.3
//Даны три точки A, B, C на числовой оси. Точка C расположена
//между точками A и B. Найти произведение длин отрезков AC и BC

#include <iostream> // подключение необходимых библиотек
#include <math.h>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Russian"); // подключение возможности чтения русского языка
	int x1, x2, x3, d1, d2, p;
	cout << "Enter A, B and C" << endl;
	cin >> x1 >> x2 >> x3; //Ввод переменных
	if (x1 < x3 < x2)
	{
	d1 = x3 - x1; //вычисление расстояний
	d2 = abs(x3 - x2);
	p = d1 * d2;
	cout << "AC = " << d1 << endl << "BC = " << d2 << endl <<  "AC*BC = " << p; //вывод результата 
	}
	else
	{
	cout << "C должна располагаться между точками A и B" << endl;
	}
}
