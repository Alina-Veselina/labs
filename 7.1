//7.1
//Дано значение угла α в градусах (0 < α < 360). Определить значение этого же угла в радианах, учитывая, что 180◦ = π радианов.
#include <iostream>// подключение необходимых библиотек

using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	float a; //объявление переменных
	cout << "Введите угол в градусах 0 < a < 360: ";
			cin >> a; //ввод переменной
			cout << "Угол" << a << " в градусах равен " << a*3.14 / 180 << " в радианах." << endl; //вывод результата
}
